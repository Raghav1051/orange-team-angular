// import { async, ComponentFixture, TestBed } from '@angular/core/testing';
// import { By }           from '@angular/platform-browser';
// import { DebugElement } from '@angular/core';
// import {SpyLocation, MockLocationStrategy  } from '@angular/common/testing';
// import {BrowserTestingModule, platformBrowserTesting } from '@angular/platform-browser/testing';
// import {BrowserDynamicTestingModule, platformBrowserDynamicTesting } from '@angular/platform-browser-dynamic/testing';
// import { FormsModule }   from '@angular/forms';
// import { NO_ERRORS_SCHEMA }          from '@angular/core';
// import { Component, Compiler}    from '@angular/core';
// //import { Router, RouterLinkWithHref, RouterLink, RouterLinkActive, RouterOutlet  } from '@angular/router'
// import {SerachPhysicianService}  from './search.physician.service';
// import {SearchModel} from './searchmodel';

// export class {
//     public navigateByUrl(url: string) { return url; }
// }
// describe('SerachPhysicianService ', function () {
//   let
//   let de: DebugElement;
//   let comp: SearchPhysicianService;
//   let fixture: ComponentFixture<SerachPhysicianService>;
//
//   beforeEach(async(() => {
//    TestBed.configureTestingModule({
//        imports:[ FormsModule],
//       declarations: [ SerachPhysicianService ],
//        providers: [
//         { provide: Router, useValue: routerStub },
//          { provide: SerachPhysicianService, useValue: SerachPhysicianService }]
//     })
//     .compileComponents()
//     .then(() => {
//       fixture = TestBed.createComponent(SerachPhysicianService);
//       comp    = fixture.componentInstance;
//       // de = fixture.debugElement.query(By.css('h1'));
//     });
//
//
//   it('should create Service object', () => {
//     expect(SearchModel).toBeDefined();
//   });
//
//   it()it('should create Service object', () => {
//     let mod: SearchModel;
//     expect(getSearchedPhysician().mod).toBe();
//   });

// describe('Search Physicians', () => {
//   let Service: SerachPhysicianService;
//   //let loc: location;
//   let model: SearchModel;
//
//
//
//
//   beforeEach(async(() => {
//     TestBed.configureTestingModule({ // To override the default providers, directives, pipes, modules of the test injector  which are defined in test_injector.js
// declarations:[SerachPhysicianService]
      //providers: [
        // Here we have to provide the classes for component dependencies
        //{ provide: SerachPhysicianService, useValue: SearchPhysician },
        // { provide: Router, useValue: DummyRouter },
        // { provide: RouterOutlet, useValue: RouterOutlet },
        // { provide: RouterOutletMap, useValue: RouterOutletMap }
      //]
  //   })
  // }));
  //
  // it('Generate the result from Gender, city', () => {
  //   expect(Service.getPhysicians().getSearchedPhysician().Physician[].model).toEqual("Indio", "male", null, null, null);
  // });
  //
  // it('Generate the results from Gender, zipCode', () => {
  //   expect(Service.getPhysicians().getSearchedPhysician()).toBe(92201, Male, null, null);
  // });
  //
  // it('Generate the results from Gender, State', () => {
  //   expect(Service.getPhysicians().getSearchedPhysician()).toBe(California, Male, null, null);
  // });
  //
  // it('Generate the results from Gender, city', () => {
  //   expect(Service.getPhysicians().getSearchedPhysician()).toBe(Indio, Female, null, null);
  // });
  //
  // it('Generate the results from Gender, zipCode', () => {
  //   expect(Service.getPhysicians().getSearchedPhysician()).toBe(92201, Female, null, null);
  // });
  //
  // it('Generate the results from Gender, State', () => {
  //   expect(Service.getPhysicians().getSearchedPhysician()).toBe(California, Female, null, null);
  // });
  //
  // it('Generate the results from Female', () => {
  //   expect(Service.getPhysicians().getSearchedPhysician()).toBe(null, Female, null, null);
  // });
  //
  // it('Generate the results for Male', () => {
  //   expect(Service.getPhysicians().getSearchedPhysician()).toBe(null, Male, null, null);
  // });
  //
  // it('Generate the results from Default', () => {
  //   expect(Service.getPhysicians().getSearchedPhysician()).toBe(null, Male, null, null);
  // });
// });
