import {SearchResult} from "./app.searchresultcomponent";
import { Router, ActivatedRoute, Params } from '@angular/router';
import { SerachPhysicianService } from './search.physician.service';

describe('SearchResult Tests', () => {
  var service:SerachPhysicianService;
    var service:SerachPhysicianService = new SerachPhysicianService();
    //let component:SearchResult = new SearchResult()
    it('Should return a list of doctors by last name and gender', () => {

        expect(false).toBeTruthy();
    });
});
